class uberCard extends payment
{
    constructor(id,number,cvv,exp_date)
    {
        super(id);
        this.number = number;
        this.cvv = cvv;
        this.exp_date = exp_date;
            
    }

  
}